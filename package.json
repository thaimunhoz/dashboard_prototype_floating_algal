{
  "name": "floating-algal-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:public": "vite --mode vercel",
    "build:vercel": "vue-tsc --noEmit -p tsconfig.app.json && vite build --mode vercel",
    "build": "npm run typecheck && vite build",
    "preview": "npm run build && vite preview",
    "typecheck": "vue-tsc --noEmit -p tsconfig.app.json && vue-tsc --noEmit -p tsconfig.worker.json && vue-tsc --noEmit -p tsconfig.node.json",
    "summary": "node scripts/build-summary.mjs",
    "seed:local": "node scripts/seed-local-r2.mjs",
    "manifest": "node scripts/make-upload-manifest.mjs",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types worker/worker-configuration.d.ts"
  },
  "dependencies": {
    "maplibre-gl": "^6.11.2",
    "shpjs": "^6.1.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.59.0",
    "@cloudflare/workers-types": "^5.20260921.1",
    "@turf/area": "^7.2.0",
    "@types/shpjs": "^3.4.7",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.0",
    "vue-tsc": "^2.0.0",
    "wrangler": "^4.138.0"
  }
}
